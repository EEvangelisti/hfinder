{
    "d": {
        "mode": "train",
        "type": "str",
        "long": "tiff_dir",
        "default": "data",
        "help": "Path to the folder containing the training image files (TIFF format)."},
    "t": {
        "mode": "*",
        "type": "str",
        "long": "auto_threshold",
        "default": "auto",
        "help": "Thresholding strategy: {auto, otsu, isodata, yen, li, triangle} or numeric (percentile <1, absolute ≥1). ‘auto’ selects Otsu or Triangle based on skewness."},
    "min": {
        "mode": "preprocess|train",
        "type": "int",
        "long": "min_area_px",
        "default": 20,
        "help": "Remove contours smaller than this area (pixels²); raise it to suppress speckles and debris."},
    "dist" : {
        "mode": "preprocess|train",
        "type": "int",
        "long": "min_distance",
        "default": 2,
        "help": "Minimum Euclidean spacing between seed peaks (in pixels) before watershed; peaks closer than this are merged. Lower values split touching objects more; higher values yield fewer splits."},
    "w" : {
        "mode": "preprocess|train",
        "type": "bool",
        "long": "watershed",
        "default": 0,
        "help": "Enable watershed splitting of touching objects after binarization; helps separate merged instances."},
    "s" : {
        "mode": "train",
        "type": "int",
        "long": "size",
        "default": 640,
        "help": "Target size (in pixels) for training images."},
    "e": {
        "mode": "train",
        "type": "int",
        "long": "epochs",
        "default": 100,
        "help": "Total number of complete training passes through the entire dataset."},
    "m": {
        "mode": "train",
        "type": "str",
        "long": "model",
        "default": "yolov8n-seg.pt",
        "help": "Path to the YOLO model file to be used as the starting point for training (pretrained weights or a custom checkpoint)."},
    "b": {
        "mode": "*",
        "type": "bool",
        "long": "debug_mode",
        "default": 0,
        "help": "Enable or disable debug mode to display additional logs and diagnostic information."},
    "v": {
        "mode": "train",
        "type": "float",
        "long": "validation_frac",
        "default": 0.2,
        "help": "Fraction of the dataset to reserve for validation during training (value between 0 and 1)."}
}
